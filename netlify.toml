[build.environment]
  PYTHON_VERSION = "3.11.4"

[build]
  command = "cd cinema_buff && pip install --upgrade pip setuptools wheel && pip install -r ../requirements-deployment.txt && python manage.py collectstatic --noinput"
  publish = "cinema_buff/staticfiles"
